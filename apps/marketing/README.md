# marketing

The public `openthink.run` site. Cloudflare Pages.

Built in iteration 2 as a static Next.js / Vite + React export. Currently the
platform app's landing screen (`apps/platform/src/web/screens/Landing.tsx`)
serves the same content under `/`.
